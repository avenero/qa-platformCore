openapi: "3.0.3"
info:
  title: "API Without HTTP Methods"
  version: "1.0.0"
paths:
  /users:
    parameters:
      - name: page
        in: query
        schema:
          type: integer
  /products:
    parameters:
      - name: limit
        in: query
        schema:
          type: integer
